FastAPI==0.111.0
uvicorn==0.30.1
pydantic==2.7.4
python-multipart==0.0.9
Celery==5.4.0
redis==5.0.6
jinja2==3.1.4
reportlab==4.2.0
networkx==3.3
passlib==1.7.4
# Note: For forensic parsing, dependencies can heavily depend on OS and environment.
# We'll include these as placeholders for a full system. 
# In a real environment, you'd compile pytsk3. For this example, we mock logic to fit the hackathon scenario.
# pytsk3
# python-ntfs
